# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db
.claude/

# Env
.env
.env.*
!.env.example

# Video compression artifacts (originals backup + optimized output).
# Kept OUTSIDE public/ so Vite never ships them. See scripts/compress-videos.mjs.
.video-backup/
.video-optimized/
*.bak

# Bundle del CMS, vendorizado en cada build por scripts/vendor-cms.mjs.
# La versión se fija en package.json; el archivo no se versiona (2 MB).
public/admin/sveltia-cms.js
